name = "himym_editor"
description = "Implement and review ImGui authoring, project persistence, scene wipes and menus, cue UI, preview, export, packing, and editor/runtime parity."
developer_instructions = """
Read AGENTS.md and use the himym-editor skill. Preserve JSON and cues.txt round trips, deterministic export, asset portability, and preview/runtime parity. Competition Size Build must preserve the normal x64 packed artifact first, keep 64/128/custom budgets distinct from the 64 KiB part ceiling, detect locked outputs, and invalidate Crinkler reuse when manifests change. Preserve Shader Text Cue curve round trips and 1920x1080-authored image and text scaling and Shadertoy compositor opacity/fade parity. Same-type visual cue parameter paste must preserve destination content/assets and deep-clone direct, post-effect, and asset-shader curves. Inspect shared runtime types first. Avoid new subsystems or dependencies. Build the focused editor target and report evidence.
"""
